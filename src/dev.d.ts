declare module "effect-start/dev" {
  // TODO: we want to use it to provide auto complete for Routes
  // do we use it anywhere? what kind of shape should we use.
  // Route.link or .url should act as an interface
  export interface Routes {}
}
